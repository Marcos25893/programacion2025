package Clases;

public class String {
    public static void main(String[] args) {

        /*
// 1. length(): Obtiene la longitud de la cadena (número de caracteres).
        int length = myString.length(); // Retorna la longitud de la cadena

// 2. charAt(int index): Obtiene el carácter en una posición específica.
        char c = myString.charAt(2); // Devuelve el carácter en el índice 2

// 3. substring(int start): Devuelve una subcadena desde el índice "start" hasta el final.
        String sub = myString.substring(5); // Devuelve una subcadena desde el índice 5 hasta el final

// 4. substring(int start, int end): Devuelve una subcadena entre los índices "start" y "end".
        String sub2 = myString.substring(2, 5); // Devuelve una subcadena entre los índices 2 y 5

// 5. indexOf(String str): Encuentra la primera ocurrencia de la subcadena "str".
        int index = myString.indexOf("java"); // Retorna el índice de la primera aparición de "java"

// 6. lastIndexOf(String str): Encuentra la última ocurrencia de la subcadena "str".
        int lastIndex = myString.lastIndexOf("java"); // Retorna el índice de la última aparición de "java"

// 7. equals(String str): Compara dos cadenas para ver si son iguales.
        boolean isEqual = myString.equals("Java"); // Retorna true si las cadenas son iguales

// 8. equalsIgnoreCase(String str): Compara dos cadenas sin tener en cuenta mayúsculas o minúsculas.
        boolean isEqualIgnoreCase = myString.equalsIgnoreCase("java"); // Retorna true si son iguales sin importar el caso

// 9. toLowerCase(): Convierte la cadena a minúsculas.
        String lowerCase = myString.toLowerCase(); // Convierte todo el texto a minúsculas

// 10. toUpperCase(): Convierte la cadena a mayúsculas.
        String upperCase = myString.toUpperCase(); // Convierte todo el texto a mayúsculas

// 11. trim(): Elimina los espacios en blanco al inicio y al final de la cadena.
        String trimmed = myString.trim(); // Elimina los espacios en blanco al inicio y al final

// 12. contains(CharSequence sequence): Verifica si la cadena contiene una subcadena.
        boolean contains = myString.contains("java"); // Retorna true si la cadena contiene "java"

// 13. replace(CharSequence target, CharSequence replacement): Reemplaza todas las ocurrencias de una subcadena.
        String replaced = myString.replace("Java", "C++"); // Reemplaza "Java" por "C++"

// 14. split(String regex): Divide la cadena en partes, basadas en el delimitador.
        String[] words = myString.split(" "); // Divide la cadena en palabras separadas por espacios

// 15. startsWith(String prefix): Verifica si la cadena comienza con una subcadena específica.
        boolean startsWith = myString.startsWith("Ja"); // Retorna true si la cadena empieza con "Ja"

// 16. endsWith(String suffix): Verifica si la cadena termina con una subcadena específica.
        boolean endsWith = myString.endsWith("va"); // Retorna true si la cadena termina con "va"

// 17. valueOf(Object obj): Convierte cualquier objeto en su representación en cadena.
        String strFromInt = String.valueOf(123); // Convierte el entero 123 a cadena "123"


         */
    }
}
